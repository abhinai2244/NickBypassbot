# NickBypassBot dependencies.
#
# The bot frontend is OPTIONAL. The CLI prank mode needs nothing but the
# Python standard library.

# Optional: local Telegram bot frontend only.
python-telegram-bot>=20.0

# Optional: load configuration from a local .env file. Not required;
# main.py also reads real environment variables.
python-dotenv>=1.0.0
